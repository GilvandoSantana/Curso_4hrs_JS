//tem um cliente, 100 premium, caso contrário, comum
let primeirosPontos = 100;
let primeiroCliente = primeirosPontos > 100 ? 'premium' : 'comum';
console.log(primeiroCliente)

let segundosPontos = 200;
let segundoCliente = segundosPontos > 100 ? 'premium' : 'comum';
console.log(segundoCliente)